{
  "name": "banner-ops",
  "version": "1.0.0",
  "private": true,
  "description": "BannerOS — a mini banner management platform",
  "scripts": {
    "api": "cd api && node src/index.js",
    "dashboard": "cd dashboard && npm run dev",
    "docs": "cd docs && npm run dev",
    "seed": "cd api && node src/seed.js",
    "install:all": "npm install && cd api && npm install && cd ../dashboard && npm install && cd ../docs && npm install && cd ../mcp-server && npm install",
    "dev": "concurrently \"npm run api\" \"npm run dashboard\" \"npm run docs\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
